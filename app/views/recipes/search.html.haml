.container
  .row
    .col-md-8
      %h2 「#{@keywords}」のレシピ

  - @recipes.each do |recipe|
    .row
      .col-md-2
        = link_to image_tag(recipe.image.url(:thumb), size: "105x168"), recipe
      .col-md-6
        %h4= link_to recipe.title, recipe
        %p= recipe.catch_copy
    %br

  = paginate @recipes, params: { contoller: 'recipe', action: 'search' }
